# Задание 4 — Распределённая обработка массива с использованием MPI
## 1. Цель задания

Нужно реализовать программу с использованием MPI, которая:

1. Создаёт массив данных N

2. Разделяет массив между несколькими процессами (2, 4, 8)

3. Каждый процесс выполняет локальные вычисления над своей частью

4. Результаты собираются обратно в один массив

5. Выполняются замеры времени выполнения для разного числа процессов

## 2. Что считается “обработкой массива”

В этой работе обработка — это простая операция над каждым элементом:

𝑦[𝑖]=𝑎⋅𝑥[𝑖]+𝑏

Эта операция выбрана потому что:

- каждый элемент считается независимо → удобно распараллеливать,

- легко проверить корректность,

- хорошо показывает влияние разбиения по процессам и накладных расходов MPI.

## 3. Общая схема работы программы

Программа выполняется в size MPI-процессах (rank = 0..size-1).

### Основные этапы:

1. rank 0 создаёт полный массив x_global размера N и заполняет случайными числами

2. Массив делится на части: каждому процессу назначается количество элементов counts[rank] и смещение displs[rank]

3. Рассылка данных выполняется через MPI_Scatterv

4. Каждый процесс локально считает y_local

5. Сбор результата выполняется через MPI_Gatherv

7. Замер времени берётся через MPI_Wtime()
